// calculate the value of pi from the infinite series
// pi = 4 - 4/3 + 4/5 -4/7+4/9 - 4/11 + ...
// Write a program which reads in a positive integer n (i.e., n >=1)
// and calculates the value of pi by adding up the first n terms of the above series




/*  错误的写法：
# include <stdio.h>
# include <stdlib.h>


int main(){

    float n;

    float x = 0.000;

    printf("Enter the value of n terms: \n");

    scanf("%f", &n);

    for(int i = 1; i <= n; i++){

        if (i % 2 == 1){

            x += 4/(2*i-1);

        }else if (i % 2 == 0){

            x -= 4/(2*i-1);

        }
        //错误的写法，出不来最后的精确值（因为 x 是 float i 是int， 最后除完得int）

    }
    printf("the value of pi is %f", x);

    return 0;
}


*/

// 下面的是正确的写法
// calculate the value of pi from the infinite series
// pi = 4 - 4/3 + 4/5 -4/7+4/9 - 4/11 + ...
// Write a program which reads in a positive integer n (i.e., n >=1)
// and calculates the value of pi by adding up the first n terms of the above series

# include <stdio.h>
# include <stdlib.h>
# include <math.h>


int main(){

    int n;

    float pi = 4.000;

    float denometer = 3;

    int i = 1;

    printf("Enter the value of n terms: \n");

    scanf("%d", &n);

    while (i < n){

        pi += 4/(denometer*pow(-1,i));

        denometer = denometer + 2;

        i++;
    }
    printf("the value of pi is %f", pi);

    return 0;
}
