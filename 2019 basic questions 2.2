// 写一个function去验证一个square matrix 是否对角线上面的每个数字的绝对值，都大于这一行其他数字加在一起的总和
// 如果是大于其他数字的总和，说明它是一个diagonal dominant matrix 因此返回值 1
// 如果不大于， 返回值为 0

# include <stdio.h>
# include <stdlib.h>
# include <math.h>
# define N 3

int is_diag_dom(int mat[N][N]){

    int flag = 0, diag = 0, sum_row = 0;

    for(int i = 0; i < N ; i ++){

        diag = abs(mat[i][i]);

        for (int j = 0; j < N; j ++){

                sum_row += abs(mat[i][j]);

        }

        sum_row -= diag;

        if (diag > sum_row){

            flag = 1;

        }

    }

    return flag;

}

int main(){

    int a[][N] = {

        {-4,2,1},
        {1,6,2},
        {1,-2,5},

    };

    printf("a is %d \n", is_diag_dom(a));

    int b[][N] = {

        {-2,2,1},
        {1,3,2},
        {1,-2,0},

    };

    printf("b is %d ", is_diag_dom(b));

    return 0;
}
